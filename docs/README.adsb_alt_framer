This is the adsb_alt_framer-write-a-block package meant as a guide to building
out-of-tree packages. To use the adsb_alt_framer blocks, the Python namespaces
is in 'adsb_alt_framer', which is imported as:

    import adsb_alt_framer

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(adsb_alt_framer)
